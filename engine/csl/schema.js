// Canonical Surface Layer Schema Definitions

/**
 * NodeSurfaceSpec defines the structural shape of a surface.
 * NOTE: This is documentation only. No runtime types are exported.
 *
 * Shape:
 * {
 *   id: string,
 *   type: string,
 *   tags: string[]
 * }
 */

export const NODE_SURFACE_FIELDS = ['id', 'type', 'tags'];
