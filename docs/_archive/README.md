# _archive — spent scaffolding & superseded docs

Docs kept for history but **out of the live set**, so they stop competing for attention (see the *gardening loop*
in [`../THE_BUILD_SYSTEM.md`](../THE_BUILD_SYSTEM.md)). Nothing here is current; when an archived doc conflicts
with a live one, **the live one wins**. Recover anything with `git mv docs/_archive/<file> docs/`.

## Archived 2026-06-24 (meta-refactor gardening run)

- `WORKFLOW.md` — superseded by `PROMPT_ARCHITECTURE.md` (old team/PR model).
- `PACKET_H52_PROMPT.md` … `PACKET_H56_PROMPT.md` (+ `_FIXFORWARD`), `WORKER_PROMPT_rung1.md` — spent one-off
  worker hand-off prompts; the packets they drove are closed.
- `PRE_SESSION_PUNCHLIST.md` — a dated session-findings snapshot, worked through.
- `VICTORY_GATES.md`, `VICTORY_GATE_v1.md`, `VICTORY_LADDER_MVP_v1.md`, `UI_VICTORY_GATES.md`,
  `SPATIAL_EXPLORATION_VICTORY_GATES.md`, `AI_PLAYTEST_ERA_VICTORY_LADDER_v1.md`, `VICTORY_GATES.log` — the early
  gate/ladder milestone framing, each self-marked superseded by `NORTH_STAR.md` / `SLICE_PLAN.md`. *Note:
  `SPATIAL_EXPLORATION_VICTORY_GATES` still contains un-built future spatial design intent — lift it into a live
  Spec/Roadmap before relying on it.*

The live gate that did **not** move: `../victory-gates/API_ACTIVATION_GATES.md` (an active pre-flight gate for
enabling an external LLM key).
