// Bestiary catalog — STANDARD tier (CR ~2–3).
// Placeholder restored to repair a broken CM11 import (see trivial.js).
// Populate with tier-appropriate creature defs (shape: see goblin.js).

export const standard = [];
