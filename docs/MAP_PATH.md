# MAP_PATH — the full path to the completed map

**Status:** specced 2026-06-27 (Tim's ask: run the map as a parallel work path).
**Vision:** [[project_map_3d_tactical_vision]] · `docs/ONE_MAP.md` (2D camera) · `docs/GRAPH_PAPER_UI.md` (aesthetic).

> ### MAPNINJA STATUS (resume here)
> - **Where we are (2026-06-30) — ROOF-PEEL CUTAWAY: zoom into a building and the roof comes off, XCOM-style (v0.13.0 / build 016, "roof peel" — landed on v2-polish).** Settlement buildings in `public/map/render3d.js` are no longer sealed solids — they're MODULAR (separable roof + 4 walls + floor + a bare interior), and the continuous zoom now PEELS the focused one open. All pure view, deterministic from the node seed, engine untouched.
>   - **Modular `makeBuilding`:** roof, 4 wall-groups (each panel + its decorations, with a unique *transparent* material set), a wooden floor, and a `makeInterior` (floor + an emissive hearth that never leaks through the closed shell + a table + stool) — instead of one box. `dimGroup` now tints unique mats IN PLACE (clones only the `SHARED_MATS`) so it can't break the cutaway's material refs.
>   - **The cutaway driver (`updateCutaway`, per frame):** the zoom depth is the existing continuous-zoom signal — `setCamera`'s `pxPerTile`, captured as `zoomPx`. Peel amount `zp = smooth(PEEL_START_PX 2400, PEEL_FULL_PX 4000, zoomPx)` (≈ node-tile zoom z 2.4→4.0 — the map opens at z=2.0, so the band must complete at a REACHABLE mid-zoom; v0.13.0 shipped 6200/z≈6.2 which was only visible near max zoom → fixed in v0.13.1, guarded by U294). The FOCUSED building = the peelable nearest the camera look-at (`target`), within ~20wu. Only it peels (`tgt = focused ? zp : 0`), each building lerping its own `cur` toward target (0.16/frame → no pop on focus change or zoom). Roof: lift `+cur*2.6` + fade + drop shadow/visibility. Walls: **camera-relative** — `op = 1 − cur·smooth(0.05,0.5, max(0, worldNormal·dirToCamera))`, so the 1–2 walls facing the camera fade while the FAR walls stay solid (it still reads as a building). World centres + wall world-normals are resolved once after `scene.updateMatrixWorld(true)`.
>   - **THE INTERFACE for the next adopters (chapel / forest huts / the rest):** a builder makes pieces modular and pushes `{ bgroup, roof, roofMat, roofBaseY, walls:[{group, n:[nx,nz], mats[], castSet[]}], cur:0 }` into the closure `peelables[]` (roof/wall mats must be `transparent` clones). After layout, the resolver fills each entry's `center` + per-wall `worldPos`/`worldNormal`. `updateCutaway()` then drives it for free off the zoom — no new camera, no per-builder peel code. To opt a builder in: emit modular pieces + register. Tune `PEEL_START_PX`/`PEEL_FULL_PX`/`FOCUS_R2`/`ROOF_LIFT` at the top of `updateCutaway`.
>   - **Verified live (visual gate, real game in play — 3-frame motion evidence):** zoom into Aldermere's central inn → roof ON (z≈2) → mid-peel (z≈3.5, roof lifting + front wall fading) → roof OFF (z≈5, interior revealed: floor, glowing hearth, table; camera-side walls gone, far walls + the neighbouring red-tile house's roof still solid) → zoom out → roof returns. Smooth, reversible, per-building. **No regressions:** the lush diorama, the 2D⟷3D zoom morph, the 2D parchment plan, the combat board (Garr vs Lingerer), the figures, and the corner HUD all still work; **2D fallback** (WebGL off) verified — no 3D mounts, fully usable, zero errors. Clean boot. `npm run check` GREEN (9030/0, convergence 109/109, determinism U19/21/22/27/30).
>   - **Honest seam / next:** ONE building type opens (settlement dwellings/inn at Aldermere) — NOT multi-floor descent, NOT NPC-populated interiors, NOT the chapel/forest interiors, NOT the combat-board buildings. Those adopt the interface above next.
> - **Where we are (2026-06-30) — LUSH MAP: the live 3D diorama now matches the outpost.html demo (v0.12.0 / build 015, "lush map" — *version bump flagged for Basecamp to land*).** The in-play 3D diorama stopped being a crude placeholder (flat green plane, plain boxes, giant floating labels). `public/map/render3d.js`'s `mountSlice3D` was rebuilt to PORT `public/map-proto/outpost.html`'s stylized techniques — DATA-DRIVEN over the real engine node positions/types, deterministic from each node's seed (pure view; engine untouched). What changed:
>   - **Vertex-coloured terrain heightfield** replaces the flat plane — gentle rolling hills FLATTENED where the world is inhabited (node centres + the roads between them) so buildings sit level; grass with dirt along the roads, rock on the high points, a forest tint past the settled tiles. Roads are now dirt ribbons that FOLLOW the terrain height.
>   - **Per-node dressing keyed to nodeType:** *settlement* → a ring of varied stylized buildings (plaster bodies, timber framing, doors + warm-lit windows, **red-tile / thatch / shingle** roofs, inn/smithy/chapel extras) around a well, wrapped in a **palisade of instanced log stakes** with a gate gap, trees outside the wall; *wilderness* → dense **low-poly icosahedron woods** (green + autumn); *dungeon_entrance* → a **stone chapel/ruin** (steep shingle roof + spire, arched mouth with a cold glow, leaning gravestones, bare trees). Undiscovered nodes dim.
>   - **Giant 3D text labels GONE** → a **clean DOM HUD** (corner title + kind, e.g. "ALDERMERE · SETTLEMENT", "THE GREENWOOD · WILDERNESS", "THE HOLLOWED CHAPEL · RUIN") that names the place the camera is looking at and updates on pan, plus a vignette + gold corner brackets. It lives INSIDE the 3D layer so it fades with the zoom morph and never blocks input (pointer-events:none).
>   - **Atmosphere:** warmer fog, the existing warm sun + bloom + ACES tone-mapping kept. Trees/stakes are **instanced** so it stays cheap and smooth.
>   - **Verified live (visual gate, real game in play — screenshots):** Aldermere opens as a lush fortified settlement (red-tile + thatch buildings, palisade ring, trees, figurine, clean corner HUD); panning east shows The Greenwood as low-poly woods and The Hollowed Chapel as a spired ruin with gravestones — each correctly named in the HUD. **No regressions:** the continuous 2D⟷3D zoom morph (out to the parchment plan, in to the diorama), the combat board (Garr vs Lingerer), and the figures all still work. **2D fallback** (WebGL forced off) verified — the 3D never mounts, the map stays fully usable in 2D at every zoom, zero errors. Clean boot. `npm run check` GREEN (9030/0, convergence 109/109, determinism U19/21/22/27/30 — engine untouched).
>   - **Honest seam / next:** STYLIZED low-poly is the bar here (Tim's "lifelike/solid-realism" minis are a separate later frontier); the diorama dresses the OVERWORLD node graph, not yet building interiors. The combat-board aesthetic + the 2D parchment plan were intentionally left as-is (out of scope).
> - **Where we are (2026-06-30) — THE ONE MAP IS NOW THE ALWAYS-PRESENT PLAY SURFACE (v0.11.0 / build 014, "one map" — *version bump flagged for Basecamp to land*).** The continuous map (2D plan ⟷ 3D diorama) — and the battle board in combat — are no longer reached through gear → Map; they are embedded DIRECTLY in `renderPlay` as the primary surface, sized to ~60% of the viewport and **opening in the 3D (tilted) view**, not the flat plan. The settings detour is gone. Four pieces:
>   - **`public/v1.js` `renderPlay`** — the in-play map is now `inCombat ? renderCombatBoard(w,{height:'100%'}) : renderContinuousMap(w,{playerPos:ui.place, initialZoom:2.0, heightCss:'100%'})`, wrapped in a `.play-map-3d` box with a tap-to-expand `⤢` (the ONLY surviving path to the fullscreen `ui.screen='map'`). The local-walk sim still runs underneath: `renderWalkPlace(w)` is still CALLED for its side-effects (it maintains `placeCtl` + `ui.place`, which the compass and the map's player marker read) — its compact canvas is just discarded. The gear "Map" item is removed. `render()`'s WebGL-teardown now keeps the 3D alive on BOTH the map and play screens (`ui.screen !== 'map' && ui.screen !== 'play'`).
>   - **`public/map/oneMap.js` + `continuousMap.js`** — new `opts.heightCss` (fill mode: the canvas fills its container instead of a fixed px height, with a `ResizeObserver` keeping the backing store crisp) and `opts.initialZoom` (seeds the camera into the 3D band so the map opens tilted, not flat). The standalone Map screen path is byte-for-byte unchanged (no opts → old fixed-height behaviour). **`combatView.js`** gained an `opts.height` override so the embedded board fills its box.
>   - **Layout (`styles.css`):** `.play-map-3d` is a `58vh` box that on desktop is allowed to SHRINK (`flex:0 1 auto`) so the narration transcript keeps a readable floor (`min-height:130px`) — the U292 anti-collapse invariant, now for the new primary map. New guard test **U293**.
>   - **CHROME REMOVED (2026-06-30, Tim's call — the DM is the only verb):** the **N/S/E/W compass buttons**, the **HP/STR Diablo orbs**, and the **"what you're wielding" kit chips** are gone from `renderPlay`. You act by TALKING; vitals/kit live in the side panels + the fiction. Movement stays text-first — typing "north" / "go west" still nudges you (the `placeWalk` move-parser path at the bare-direction handlers is kept; only the *buttons* are gone). This clears the play column so the map sits at its full **~58% basis in escape mode too** (was ~40% when the compass+orbs+kit ate the column), with a generous ~217px transcript. `renderDiabloOrbs` import dropped.
>   - **Verified live (visual gate, real game — screenshots):** play opens on the **3D tilted Aldermere diorama** with the "You" figurine (no gear step, no compass/orbs/kit — just map + narration + input) → scroll OUT to the **2D parchment region plan** → back IN to 3D → loaded a `beginCombat` save → the **3D battle board** (Garr vs Lingerer) is right there in play at 58% → **WebGL forced off → clean 2D fallback board**, fully playable. Zero console errors throughout. `npm run check` GREEN (9030/0, convergence 109/109, determinism U19/21/22/27/30 — pure view, engine untouched).
>   - **Honest seam / next:** SURFACING the existing continuous map in play — NOT unifying the local-walk renderer into the zoom (deeper follow-on; the walk sim still runs underneath for the player marker + typed movement).
> - **Where we are (2026-06-29) — COMBAT MINIS ON THE TACTICAL BOARD (v0.8.0 / build 011).** During a live fight the Map becomes the XCOM board: a gridded w×h plane with the player + enemy minis standing on their REAL engine cells (`world.combat`, the MX-1 grid that landed f4e1205). The version finally bumps because this is visible in the playable build. Three pieces:
>   - **`public/map/combatScene.js`** (new, pure selector) — `combatSceneFromWorld(world)` → `combat-scene/v1` `{ schema, active, grid{w,h}, player{cx,cy,name}, enemies[{id,name,cx,cy,defeated}] }`. A PURE read of `world.combat` (+ the party avatar's name); zero engine writes, no randomness, cells clamped to grid bounds. cx=east / cy=south — SAME convention as sliceScene.js. This is the seam talk→token (Step 4) builds on.
>   - **`render3d.js` `mountCombat3D(container, scene, opts)`** — a gridded dais (board base + grass top + exact-coord grid LineSegments) with minis at cell centres `((cx+0.5)·CELL_WU, (cy+0.5)·CELL_WU)`. Player = the blue/gold "You" token (reused) on a highlighted cell; enemies = red spiky tokens; **a defeated foe renders toppled + dimmed**. Oblique XCOM 3/4 camera, drag-to-orbit (view-only — the board never moves anyone). **Intentionally self-contained from mountSlice3D** so the shipped overworld morph can't be regressed by combat changes. Controller: `{ dispose, renderFrame, setView({alt,az}), pause, resume, canvas }`; live handle `window.__combat3d`.
>   - **`public/map/combatView.js` + v1 surfacing** — while `world.combat.active`, `renderMap()` returns the **Battle board** (title "Battle") instead of the overworld; the overworld + continuous zoom return the instant the fight ends (UNCHANGED out of combat — verified). Mirrors the continuous-map stack: a **2D canvas board always drawn underneath** (the no-WebGL fallback + instant paint) with the **3D board overlaid opaque** when WebGL is available. Re-reads `world.combat` each render → minis show CURRENT positions (they don't move yet). Disposed on screen-change so no WebGL context leaks.
>   - **Verified live (visual gate, real engine combat via a `beginCombat` save loaded into slot1):** 12×10 board, You at (1,5) west-mid, Bandit Captain (6,7) + Woods Bandit (6,6) east, Cutthroat (6,8) rendered DOWNED — rendered cells match `world.combat` exactly, in BOTH the 3D board and the 2D fallback (WebGL forced off). Overworld morph re-verified un-regressed. Zero console errors. `npm run check` GREEN (9009/0, determinism U19/21/22/27/30 included — engine untouched, pure projection).
>   - **Honest seam / next:** render CURRENT positions ONLY — NOT moving minis (Step 4 talk→token), NOT figurine assets (Step 5 — placeholder tokens), NOT geometry-derived cover/flank (engine follow-on MX-2). The board only surfaces on the Map screen for now (not embedded in the play screen). To reproduce a fight for testing: build a world, `beginCombat(w,{enemies,reason})`, `JSON.stringify(ensureWorld(w))` into `localStorage['ai-dm-v2:slot:slot1']`, then gear → Load.
> - **Where we are (2026-06-29) — ONE CONTINUOUS-ZOOM MAP, no toggle (v0.7.0 / build 010).** The discrete 2D|3D button is GONE. The Map is now a single semantic-zoom surface: scroll out = the 2D graph-paper regional plan; scroll in past a threshold and the plane TILTS and the ink map cross-fades into the live 3D overworld diorama (and reverses smoothly on zoom-out). Zoom level alone drives the representation — ONE_MAP.md's "one surface, no tabs" made real, and the camera the minis/tactical-descent steps build on. How it's built:
>   - **`public/map/continuousMap.js`** (new) — the wrapper that owns the morph. The 2D `oneMap` canvas stays the single INPUT surface + the no-WebGL fallback; the 3D `render3d` canvas is stacked as a PASSIVE overlay (`pointer-events:none`, so all wheel/drag still go to the 2D map). On every 2D camera change (`oneMap` now fires an `onCamera` hook) it mirrors zoom→3D: cross-fade opacity `b = smoothstep(Z_3D_START 0.22, Z_3D_CROSS 0.50)`, tilt `phi` lerps top-down→oblique over `[0.22, Z_3D_TILT 2.5]`.
>   - **The seamless trick (scale-lock):** `render3d.setCamera({ phi, az:0, target, pxPerTile })` picks the camera DISTANCE that makes one node-tile cover the same screen px in 3D as in 2D (`pxPerTile = NODE_WU * z`), with `target` = the 2D camera centre (tile units) and `az:0` (north-up). So at the crossover both views share scale, centre and orientation — only the tilt diverges. The cross-fade reads as the flat map coming to life, not two scenes cut together (verified: at z≈0.35 the 3D houses + "You" token sit exactly on the 2D Aldermere marker, both labels overlapping).
>   - **render3d additions:** `setCamera` (above), `pause()`/`resume()` (idle the rAF loop while the overlay is hidden — zero GPU when zoomed out), and `controls:false` (skip its own orbit listeners; the overlay is driven, not dragged). `window.__map3d` still exposed; new verification hook `wrap.__continuousFocus(cx,cy,z)` drives the unified zoom headlessly.
>   - **Verified live (visual gate, screenshots of the real game Map):** z=0.12 flat 2D plan (Aldermere + The Greenwood) → z=0.35 cross-fade (3D aligned over the 2D ink) → z=1.5 full tilted 3D diorama → z=0.10 clean return to the 2D plan. No toggle button. Zero console errors across boot/zoom-in/zoom-out. **2D-only fallback verified:** with WebGL forced off, zooming past the threshold never mounts 3D — the map stays 2D and fully usable at every zoom (the high-zoom 2D village-layout band still renders). `npm run check` GREEN (9004/0, determinism U19/21/22/27/30 included — engine untouched, the view is a pure projection).
>   - **The camera interface for the NEXT steps (minis + tactical descent):** drive the 3D via `ctrl.setCamera({ phi, az, target:{tx,ty}, pxPerTile })` — tile-unit target, scale-locked to the 2D. The morph thresholds (`Z_3D_START/CROSS/TILT`, `PHI_TOP/OBL`) are the tuning knobs in `continuousMap.js`. Honest seam: this is the OVERWORLD 2D⟷3D zoom only — NOT yet the descent into a building interior / tactical grid, NOT minis, NOT talk→token. Those inherit this camera.
> - **Where we are (2026-06-29) — THE 3D VIEW IS NOW WIRED INTO THE LIVE GAME (v0.6.0 / build 009).** The 3D overworld stopped being a standalone proto: it is now a real, opt-in **2D⇄3D toggle on v1's Map screen**, fed LIVE by `sceneFromWorld(<running world>)` — open the Map in actual play, click **3D**, and the Aldermere region renders in 3D from real engine positions (the four places + the "You" token). Three pieces:
>   - **The mountable renderer** — `public/map/render3d.js` `mountSlice3D(container, scene, opts) → { dispose, renderFrame, setView, canvas }`. Factored out of `map-proto/slice.html`. **Lazy-loads Three.js** from the CDN via an **importmap added to `public/v1.html`** (inert until the player opts into 3D → zero first-paint cost). Probes WebGL and **throws `webgl-unavailable`** if 3D can't run. Paints one frame at mount so first paint never depends on rAF.
>   - **The v1 integration** — `public/v1.js` `renderMap()` now carries `ui.map.mode` (`'2d'` DEFAULT ⇄ `'3d'`). 3D mounts into a container fed by the pure `sceneFromWorld` read; **graceful fallback** flips back to 2D on any failure (verified by simulating no-WebGL → lands on the working 2D map, never a blank screen). The live canvas is disposed on mode-flip, screen-change, and re-render (no WebGL-context leak across v1's full-rebuild render model). 2D graph-paper is untouched.
>   - **Verified live (the visual gate):** v1.html in play → Map → 3D shows Aldermere(0,0)→The Greenwood(2,0)→The Hollowed Chapel(4,0) reading left-to-right with Crowfoot Camp(3,2) branching south — **same geography as the 2D map**, north-up (`setView({az:0})`). Zero console errors across boot/play/2D/3D/toggle/fallback. `npm run check` GREEN (9004/0, determinism U19/21/22/27/30 included — engine untouched, the view is a pure projection).
>   - **New interface for the next (combat-mini) step:** `mountSlice3D` is the mount seam; the live controller is exposed as `window.__map3d` ({ dispose, renderFrame, setView, canvas }) for headless driving + future hooks. The contract `slice-overworld-scene/v1` (`sliceScene.js`) is still the data seam. Honest seam: overworld POSITIONS + placeholder geometry only — NOT combat minis, NOT the figurine pipeline, NOT voice-spatial movement (the next MAP_PATH steps).
> - **Where we are (2026-06-29) — FIRST BRIDGE FROM PROTOTYPE TO REAL ENGINE STATE landed.** The 3D view now renders the shippable slice's OVERWORLD (the Aldermere region) from REAL engine positions, not fake hand-authored data — the first time a `map-proto` reads the live world. Three pieces:
>   - **The contract** — `public/map/sliceScene.js` `sceneFromWorld(world)`: a PURE READ of `world.map` → plain JSON `slice-overworld-scene/v1` `{ schema, seed, bounds, nodes[{id,name,nodeType,x,y,discovered}], edges[{a,b,kind}], player{nodeId,x,y} }`. Zero engine writes, never serialized/hashed. Coordinate convention: x = east(+), y = south(+) on the integer tile grid. Dumped via `scripts/dump-slice-scene.mjs` (boots `newWorld→beginAdventure` on seed `aldermere`) → `public/map-proto/slice-scene.json`. **This JSON is the documented interface the next steps build on.**
>   - **The renderer** — `public/map-proto/slice.html` (Three.js, based on `outpost.html`) FETCHES that JSON and places all four named places (settlement/wilderness/dungeon_entrance, each labeled + differentiated) + the player "You" token in 3D, with edges as roads/paths. View-deterministic: all scatter from `mulberry32(seed+nodeId)`, no `Math.random`. Headless: `window.go(ft,az,n,tx,ty,tz)` (north-up = `az:0`), `window.__ready`, `window.renderNow(n)`.
>   - **Verified on screen (2D↔3D agree):** Aldermere(0,0)→The Greenwood(2,0)→The Hollowed Chapel(4,0) east chain reads left-to-right; Crowfoot Camp(3,2) branches south off the woods — same relative geography as the live graph-paper Map tab. Engine untouched (no `engine/` files changed; determinism gates structurally unaffected).
>   - **Honest seam:** overworld POSITIONS only — placeholder geometry, NOT combat minis, NOT the figurine pipeline, NOT voice-spatial movement, and NOT wired into `v1.html` (standalone proto, so no product-version bump). Those are the next MAP_PATH steps; they inherit `slice-overworld-scene/v1` as the seam.
> - **Where we are (2026-06-27):** Tim locked the destination + figurine taste, then asked to *see* it. Two vision prototypes built (Three.js, verified live on the static `dungeon-preview` server :5180; rAF throttles headless so drive frames with `window.go(...)` then screenshot):
>   - `public/map-proto/dejarik.html` — torchlit **dungeon** diorama, knight + ogre minis-on-bases on a grid, XCOM HUD, bloom/embers, flat-map↔tactical-3D **tilt**.
>   - **D&D × XCOM locked (2026-06-28) → `docs/DND_XCOM.md` + [[project_dnd_xcom]].** Adopt XCOM's tactical brain, reject its strategic metagame. THE LAW: *narrate the read, never the number* (playable blind). Individual initiative; **permadeath**; town gen = **plot-and-parcel**. `outpost.html` updated: plot-parcel layout (fixed church-clipping-inn) + a **tactical-read HUD** (initiative · action economy · narrated read · permadeath badge, shown at street zoom). Queued engine packets DX-1..4 in the doc — NOT yet built (hot files).
>   - `public/map-proto/outpost.html` — **THE ENGINE-RULES PASS (2026-06-28, supersedes town.html for layout).** Wayfarers' Outpost regenerated by a **seeded settlement grammar** (deterministic mulberry32): roads → plots → **buildings front the street, doors to the road, roof ridge on the long axis, no overlap**. **Meander roads** that run gate↔square↔off-map (no dead-straight stubs). **Fortified-by-danger:** frontier = **timber palisade ring + gatehouses + a motte-and-bailey keep** (form encodes threat: village→palisade→star-fort ladder). **Turn-based:** world frozen; `Resolve turn` (button/space) steps every agent ONE beat over **walkable cells only** (real collision — no wall-clipping), then **recenters the camera on the player** (XCOM anchor, prevents drift into other locations). Keeps terrain/forest-outside-walls/inn-roof-reveal/chickens/orbit/descent. Drive headless: `window.go(ft,az,n,tx,ty,tz)` + `window.resolveTurn()` + `window.tick(dt,n)`. **Honest seam: the grammar runs in the page's JS, not yet `engine/`; agent movement is random-walk, not DM-driven; figures still placeholders.** Embodies Tim's notes: fortify-by-danger, no stub roads, turn-gate + recenter, frontage layout.
>   - `public/map-proto/town.html` — **THE WHOLE TOWN (2026-06-27, the first big one — open village, real-time wander).** Wayfarers' Outpost as a complete living diorama: rolling vertex-coloured terrain (grass/dirt/rock/forest biome), dirt roads, a pond, ~9 buildings (inn, cottages w/ thatch/tile/shingle roofs, chapel w/ spire, well, market stalls) all half-timbered, ~170 instanced trees (some autumn) + instanced rocks, wandering **villagers** (some walk the road, legs swing) + pecking **chickens** + a dog, golden-hour sun w/ long shadows + sky dome + fog. **Seamless descent, all one camera:** hand-drawn map (~480) → 3/4 town (~175) → street level (~24, among the chickens) → **roof-reveal into the inn** (the hollow inn's roof lifts/fades → look down into the 3D common room: bar, tables, hearth). XCOM orbit + `window.go(ft,az,n,tx,ty,tz,roof)` / `setPitch`. **Honest seam: the town LAYOUT is a hand-authored data array (`buildings`/`roads`), NOT yet from the engine's `tallow` node graph — structured to be fed by it next (worldSpace.js).** This is the visual vertical slice of the whole vision in one file.
>   - `public/map-proto/opening.html` — **the realism pass (2026-06-27).** The demo's **opening room** (Wayfarers' Outpost, seed `tallow`): detailed bed (frame/linen/blanket/pillow), **iron chest** (banded, riveted, brass padlock), stone hearth w/ fire, mullioned window + moonlight, table+candle+tankard+book, stool, barrel, sacks, shelf of bottles, rug, half-timber plaster walls, bump-mapped plank floor, you-figure. **Realism swings:** `RoomEnvironment` image-based lighting (real metal/wood reflections), firelight↔moonlight chiaroscuro, dust motes, fake contact-shadows, ACES+bloom. Same XCOM orbit + altitude zoom + grid (furniture = blocked cells). `window.go(ft,az,n)` + `window.setTarget(x,y,z)`.
>   - `public/map-proto/cathedral.html` — **the good one (2026-06-27).** A faithful **Gothic cathedral** (Notre-Dame archetype): cruciform plan, twin nave colonnades, pointed-arch arcades, apse ambulatory, rose window, stained-glass clerestory, brick+stone+timber. Adds an **XCOM camera** — drag-orbit (azimuth) + scroll-zoom (drives the altitude bands + auto tilt) + Q/E 90° snap. Grid-truth now classifies **columns as blocked cells**, aisles walkable. Drive headless: `window.go(ft, az, n)`. *Supersedes temple.html for look.*
>   - `public/map-proto/temple.html` — the **full three-altitude continuous zoom** (`window.go(ft,n)` / on-screen altimeter): **hand-drawn parchment map (~420ft) → dissolve (~330) → 2D battle map (~150) → stand-up (~48) → 3D tactical diorama (~32)**. Scene = an **organic church/temple**: a rounded **brick apse**, **angled timber side walls** (brick AND wood), altar + priest + candles + stained-glass shafts. **Answers the grid-vs-organic question:** the square tactical grid is rule-truth; walls are freeform on top; every tile is classified walkable / wall / blocked via point-in-polygon over the footprint, shown as a colored overlay so you SEE the grid coping with curves + odd angles. Grid for rules, art for the world (BG3/XCOM convention).
>   - **Honest seam:** placeholder primitive minis (knight, robed priest), a *procedural stand-in* for the real hand-drawn map renderer, and a static fake scene — NOT real engine positions or realistic models yet. The grid-classification logic is the genuinely real/portable part.
> - **Art direction (Tim, locked):** **solid living realism** for the minis — full-color, opaque, lifelike, alive (breathe + fight like Star Wars Dejarik holochess). Reference governs *behavior + staging*, not the blue-hologram finish. See [[project_map_figurine_realism]]. *This revises the earlier "stylized low-poly" note below.*
> - **What's next:** (a) raise mini fidelity toward real rigged models + the archetype-rig pipeline (one rig → many creatures, breathe/fight); (b) build the real foundation — **Phase 1.1 world-space** — so the prototype renders ACTUAL engine positions instead of fake data.
> - **Open taste calls for Tim:** mini fidelity bar for v1 (stylized-real vs near-photoreal); whether to add a faint holo-shimmer accent; how literal the XCOM HUD becomes.
**Governing laws (never violate):**
- **THE DM TEST** — the map is an *aid*, never the interface of record. You act by *talking*; the map shows the result. The camera never teleports anyone.
- **Engine owns position.** The map is a **pure projection** of engine-owned positions; it never writes game state. Determinism preserved — `worldHash` / U19·U21·U22·U27·U30 stay green. Client-side projection only (`public/map/worldSpace.js`), engine canon coords unchanged.
- **Graceful fallback.** 2D is the floor; richer views (3D) are enhancements that degrade to 2D, never hard dependencies.

---

## The destination (one paragraph)

One continuous camera, **two representations**: a **2D zoomable strategic map** for planning travel, and a **tactical view where incidents happen**. Zoom all the way in and the 2D top-down **tilts and morphs into a 3D "D&D street view"** — terrain stands up into a battle map, characters/NPCs/monsters become **minis on pedestals**. Throughout, you play by *speaking* — "Take cover behind the barrel!" moves your token to the resolved spot, with tactical consequence (cover, line of sight, flanking). The 2D ink view and the 3D diorama are **two renderers over the same positional truth**, so 3D is a renderer swap, not a rewrite.

---

## The lane model (how this parallelizes)

| Track | Touches | Parallel-safe? |
|---|---|---|
| **Map-renderer lane** | `public/map/*`, `public/v1.js`, a new WebGL module, `worldSpace.js` | ✅ **Yes** — isolated from engine hot files. This is the home of Phases 1 & 3 and the render-halves of Phase 2. **Runs as its own worktree alongside engine lanes.** |
| **Engine coordination** | `engine/state.js` (schema), `effectsCore.js`, `playloop.js`, `escapeCombat.js`, a new spatial resolver | ⚠️ **Interleaves, not pure-parallel** — Phase 2's engine pieces are determinism-sensitive competence hot files. Homebase sequences these against other engine work (schema/WORLD_VERSION discipline, serial on hot files). |

**Verification for every phase:** the visual map gate (`scripts/dm-playtest-visual.mjs`) — it *watches the rendered map*, so it is the acceptance oracle for "it registers correctly on the player map" ([[feedback_map_fidelity_rule]]).

---

## Phase 0 — where we are (done / in flight)

- ✅ Engine interior navigation coherent (VG-F1, `9821f0d`) — rooms advance, no teleport.
- ✅ Graph-paper UI substrate shipped (`GRAPH_PAPER_UI.md` Slice 1).
- ✅ Visual map gate exists (the acceptance oracle).
- 🟡 **VG-F3** (marker reads walk-pos, lags rooms/inside-outside) — **near-term patch on the CURRENT patchwork.** *Decision:* either ship VG-F3 standalone now, OR fold it into Phase 1.1 (it dissolves once the marker reads world-space). Recommend folding into Phase 1 if the lane starts soon; otherwise patch it.
- ❗ Today the "map" is **five renderers pretending to be one** (`WorldMap`/`RegionMap`/`LocalMap`/`handDrawnPlace`/interiors), each with its own coordinate system. Every boundary is a seam; the marker/fog/inside-outside bugs are seam bugs. **Phase 1 retires the genre.**

---

## Phase 1 — ONE_MAP: one coordinate space + one 2D continuous camera  ·  RENDERER LANE (parallel)

*Goal: replace the five-renderer patchwork with one world space and one Google-Maps-style 2D camera. This is the structural fix that kills the seam-bug genre.*

- **1.1 — World-space projection.** Implement `public/map/worldSpace.js` per ONE_MAP's coordinate model (nodes `node.x,y × NODE_WU=1000`; villages anchored at node centers, `PLACE_WU=4`; interiors fit to building footprint). **Pure + seeded; no engine writes.** Marker now reads engine room/interior → **subsumes VG-F3.**
  - *done-when:* every entity (node, village layout, interior, player, NPC, object) resolves to one `{wx,wy}` world-unit address; the player marker tracks the engine's current room and the inside↔outside transition; determinism green.
- **1.2 — One camera + LOD.** Unified camera `{cx, cy, z}` (center in wu, z = px/wu), wheel-zoom + drag-pan, with level-of-detail bands (county → settlement → building) so labels/detail fade in by zoom. Retire the scale tabs.
  - *done-when:* one continuous zoom from county to a single room, no tabs, no mode-switch; smooth pan/zoom.
- **1.3 — Route every surface through the one renderer.** The in-play compact map and the Map tab both render from `worldSpace` + the one camera (the old fixed-scale renderers stay on disk as fallback only).
  - *done-when:* the in-play map and the Map tab are the same renderer at different default zooms; the five-renderer seams are gone.
- **1.4 — Travel intent stays adjudicated.** Zooming out and clicking a far place is *travel intent the DM resolves* (time, roads, encounters, "you know of no such place") — the camera never teleports.
  - *done-when:* a far-click produces a DM travel adjudication, not a position write.

**Verify:** visual gate shows the marker tracking rooms + the inside↔outside flip (the VG-F3 acceptance test), at multiple zooms.

---

## Phase 2 — reactive tactical positioning, in 2D  ·  ENGINE + RENDERER (interleaves)

*Goal: prove the talk → commit → visible-token loop FLAT before any 3D. "Take cover behind the barrel" moves your dot, with crunch. This is the phase with real engine work.*

- **2.1 — Grounded positioned objects** *(ENGINE — schema; coordinate w/ engine lane).* Scenes carry real, *located* objects (the barrel is world state with a position + properties like cover-giving), not narrated flavor. Flip side of [[project_dm_invents_geography]] — you can't take cover behind a hallucinated barrel.
  - *done-when:* a furnished scene exposes positioned objects in world-space; WORLD_VERSION bumped per protocol; invariants + determinism green.
- **2.2 — Spatial-intent resolver ("spatial physics").** Propose/commit applied to space ([[IG-11]] sibling): the LLM *interprets* loose intent ("behind the barrel", "flank the wolf", "between me and him"); the **engine commits the exact deterministic cell** and adjudicates feasibility ("40 ft across open ground — you won't make it this turn"). LLM proposes, engine owns the geometry + the number.
  - *done-when:* a spoken spatial command resolves to a committed position deterministically; infeasible moves get an honest in-fiction adjudication (DM Test); never a teleport.
- **2.3 — Tactical crunch** *(escapeCombat — hot file, serial).* Position confers mechanics: cover bonus, line of sight blocks, flanking helps. The position means something.
  - *done-when:* cover/LOS/flank read from positions and affect rolls; consistent with the live combat engine.
- **2.4 — Render the moving token + relationships** *(RENDERER — parallel).* The marker animates to the committed spot; cover/LOS read on the 2D map.
  - *done-when:* the visual gate shows the dot move behind the barrel and the cover state legible on the map.

**Verify:** visual gate run where the Map Nit says "take cover behind the barrel" and the dot is seen moving there with cover applied.

---

## Phase 3 — the 3D tactical view  ·  RENDERER LANE (parallel, large)

*Goal: zoom all the way in → 2D tilts/morphs into a 3D battle diorama with minis. A renderer swap over the SAME world-space positions from Phase 1 — no new game logic.*

- **3.1 — WebGL scene over world-space.** A Three.js (or equiv) tactical renderer reading the identical `{wx,wy}` positions; camera pitches from top-down toward oblique as zoom crosses the tactical threshold.
  - *done-when:* the tactical scale renders in 3D from the same positions the 2D view uses; 2D remains the fallback.
- **3.2 — The tilt/morph transition** (the artful core — the "Google-Earth swoop"). Flat ink hands off to 3D meshes smoothly across the zoom; no jarring cut.
  - *done-when:* zooming through the threshold feels like one continuous move, not a scene change.
- **3.3 — Minis on pedestals.** Each character/NPC/monster renders as a stylized low-poly mini at its world-position. **Keyed to the bestiary** (creature type → mini), procedural / small library / AI-gen to keep the asset cost sane.
  - *done-when:* tokens become minis at the right positions; cover/movement read in 3D.
- **3.4 — Terrain meshes.** Biome data that paints 2D tiles extrudes to 3D (trees stand up, mountains rise), legible at a glance.
  - *done-when:* the tactical terrain matches the 2D biome truth in 3D.

**Guardrail (hold hard):** even in 3D street-view you **still play by talking** — never click-to-move. The 3D view is a window, same as the 2D ([[project_dm_only_verb]]).

---

## Cross-cutting (every phase)

- **Aesthetic continuity:** hand-drawn graph-paper / parchment for 2D (`GRAPH_PAPER_UI.md`) → for 3D, **solid living-realism minis in a torchlit diorama** (Tim's locked call 2026-06-27, [[project_map_figurine_realism]] — *not* the earlier "stylized low-poly"; the Dejarik reference is for the alive *behavior*, not a hologram finish). The `docs/map-style-*.html` explorations + `public/map-proto/dejarik.html` (the vision prototype) feed the look.
- **Determinism:** projection + resolver are pure & seeded; no view ever writes world state.
- **Fallback ladder:** 3D → 2D continuous → (legacy fixed-scale renderers on disk).
- **Acceptance oracle:** the visual map gate, every phase. "Not done until it registers on the player map, verified live."

---

## Sequencing & the parallel answer

```
Map-renderer lane (PARALLEL, own worktree, alongside engine lanes):
  Phase 1 (one space + 2D camera)  →  Phase 2.4 (render moving token)  →  Phase 3 (3D)

Engine coordination (INTERLEAVES — Homebase sequences vs engine lanes):
  Phase 2.1 positioned objects (schema bump)  →  2.2 spatial resolver  →  2.3 tactical crunch
```

- **Most of the map is genuinely parallel** (Phases 1 & 3 + render-halves) — start the renderer lane now.
- **Phase 2's engine pieces are the coordination point** — they touch schema/`playloop`/`escapeCombat` and go through the version-bump + serial-hot-file discipline. The renderer lane can build Phase 1 while Phase 2's engine work is scheduled.
- **3D (Phase 3) cannot start meaningfully until Phase 1's world-space exists** (it renders the same positions), but it does NOT need Phase 2 — a static 3D view of positions can land before reactive positioning.

## Open decisions (resolve before the lane starts)
1. **VG-F3 now, or fold into Phase 1.1?** (Recommend fold if the lane starts within ~a week.)
2. **3D engine:** Three.js (default) vs a lighter custom WebGL — pick at Phase 3 start.
3. **Mini asset strategy:** procedural low-poly vs small hand-made library vs AI-gen — pick at Phase 3.3.
4. **How far does "two representations" revise ONE_MAP.md?** Fold the two-representation language into ONE_MAP when Phase 1 begins.
