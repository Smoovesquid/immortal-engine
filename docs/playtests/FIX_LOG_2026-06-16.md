# Fix Log — 2026-06-16 (rung1-foundation)

Session: worker window / branch `rung1-foundation`. Gate report: docs/playtests/opus-gate-2026-06-16.md (17% fail, 8/48 turns).

| # | Class | Failed turn | Root cause | Fix | Test |
|---|-------|-------------|------------|-----|------|
| 1 | DM_TEST_DEADEND | "how do you resolve a sword swing?" / "is there a dice mechanic?" got a roll | `isMetaQuestion`/`handleMetaQuestion` only ran inside active escape-combat (playloop.js ~1706); the generic out-of-combat resolver had no meta-question gate at all, and no `META_MECHANICS` pattern existed | Added `META_MECHANICS` + `META_ADVICE` patterns to `gracefulAdjudication.js`; added an out-of-combat, out-of-dialogue meta-question gate near top of `playerMoveCore` (excludes `META_LOCATION`, which has its own tested "Exits:" explore path; excludes dialogue, where the same phrasing is often a real in-fiction NPC question — G10) | `tests/U163.metaMechanicsAdviceGate.test.js` |
| 4 | DM_TEST_DEADEND | "Should I go talk to them, or is that a bad idea?" → navigation deadend | Same root cause as #1 — no `META_ADVICE` pattern existed | Same fix as #1 | `tests/U163.metaMechanicsAdviceGate.test.js` |
| 3 | CRUNCH_INCONSISTENCY | "grab a loose roof beam, and swing it through the Outpost's front window" → tagged `[cast-consequence: person-innocent ... recoil]` with fabricated fire narration | `HOSTILE_CAST_RE` in `castConsequence.js` matched the bare verb "loose" (the adjective in "a loose roof beam"); with the Lingerer NPC present this misclassified a physical beam-swing as an offensive spell | Narrowed `HOSTILE_CAST_RE` to unambiguous casting verbs (cast/invoke/channel/conjure); removed hurl/throw/loose/sling, which `OFFENSIVE_RE` already covers explicitly for the magical case (hurl flame/fire/lightning) | `tests/U164.castConsequencePhysicalNotSpell.test.js` |
| 5 | CRUNCH_INCONSISTENCY | "Senna just told me Might is at a penalty — give me the number" → DM said "minus three", mechanics showed `stat:WITS-1` (LLM invented the number) | Same root cause as #1/#4 — `META_STAT` already existed but only ran inside combat, so this fell into a generic roll and the narrator had to make up a number | Covered by the same #1 fix (out-of-combat meta gate); no code change beyond #1 | `tests/U163.metaMechanicsAdviceGate.test.js` |
| 6 | CRUNCH_INCONSISTENCY (NOT A BUG) | Chaos-griefer fall: `[hazard:fall \| 8]`, judge said "PC HP (7/15) is not updated" | Inspected `playloop.js` out-of-combat hazard gate (~1545-1558) and `escapeCombat.js`'s in-combat hazard branch (~969-977 + commit at ~1867) — both correctly thread `resolveHazard`'s returned `hp` into `meta.escapeHp` and into the judge's post-turn `canonGroundTruth(world)` snapshot. The reported 7/15 is exactly 15 (max) − 8 (the rolled fall damage) — i.e. the damage WAS applied; the judge's note describing it as "not updated" appears to be a false read of correctly-applied math, not an engine defect. | No fix made — left as-is. Flag if it recurs with a non-matching number. | — |
| 7 | DM_ARTIFACT_LEAK (NOT A BUG) | Training-dummy swing mechanics ending `...stat:MIGHT-1 \| NA` (looked truncated/malformed) | Traced `buildMechanicsLine` (`engine/resolve.js:533-539`) — it always closes with `]`. `roll:19` with a `MIGHT-1` modifier implies a natural-20 die (20−1=19, margin 19−14=5), so the line is `...stat:MIGHT-1 \| NAT20]` — the gate report's markdown table simply truncated `NAT20]` to `NA` in the cell. The line itself is well-formed. The judge's substantive complaint ("no foe, no combat needed against a dummy") is that a harmless practice-swing got the same approach/stake/risk skill-check fields used for any adjudicated action — that's the engine's uniform resolution format by design, not a leak. | No fix made — false alarm from report formatting, not an engine defect. | — |

## Follow-up — 2026-06-16 (rung1-followup-2026-06-16)

Branch off `rung1-foundation`. The partial gate re-run (`docs/playtests/opus-gate-followup-2026-06-16.md`) confirmed the 5 originals fixed but surfaced 5 NEW issues. Same protocol. Full suite: **7726 pass / 0 fail**; determinism (U19/U21/U22/U27/U30) green.

| # | Class | Failed turn | Root cause | Fix | Test |
|---|-------|-------------|------------|-----|------|
| F1 | DM_TEST_IDENTITY | DM called the player "Garrick" then "Thorn"; dodged when called out twice | Two gaps: (a) no meta-question answered "what's my name?" / "you called me X", so the call-out fell to a roll/fiction dodge; (b) `buildDMSystemPrompt` carried `- Name: ${player.name}` but no rule forbidding the narrator inventing/swapping it | (a) `META_NAME` pattern + handler in `gracefulAdjudication.js` answers the name straight from `party[0].name`; (b) a name-pin rule in `buildDMSystemPrompt` (`llmAdapter.js`): "NEVER invent, swap, or use any other name" | `tests/U166.characterIdentity.test.js` |
| F2 | META_STAT (weapon) | "What's the damage on the Hatchet vs the Worn Blade?" / "What die does the damage roll use?" → in-fiction non-answers | Weapon damage dice (`inventory.weapons[].damage` "1d6", or the kit weapon's numeric `dmgDie`) had no meta-question coverage — distinct from `META_STAT` (ability mods). Queries fell to a dice roll / narration | `META_WEAPON_DAMAGE` pattern + `answerWeaponDamage()` in `gracefulAdjudication.js` — reads the real loadout (both `damage` strings and `dmgDie`), reports named weapons or the whole loadout | `tests/U165.weaponDamageQuery.test.js` |
| F3 | CRUNCH_INCONSISTENCY | "I dislocate my own thumb to slip the rope and grab—" read as resolving with zero dice | The escape-combat weapon-strike & attack-cantrip branches narrated the roll in the prose `beats` but never set `actionMech`, so the structured mechanics line fell back to the bare round marker `[combat:rN]` — no dice surfaced (grapple/parley/spell branches all set theirs). Display-only; no state/rng/worldHash change | `escapeCombat.js`: strike now emits `[strike:<weapon> \| atk:N vs AC:M → hit/miss \| D dmg]` (or a swing summary); cantrip emits `[cantrip:<name> \| atk:N vs AC:M → …]` | `tests/U167.strikeMechanicsDice.test.js` (+ `UX2` route classifier updated) |
| F4 | CRUNCH_INCONSISTENCY | A failed WITS check narrated as a clean success (canon names matched, prose contradicted the roll) | The narrate-polish prompt's "mechanics are AUTHORITATIVE" rule (`buildSystemPrompt`, `llmAdapter.js`) only gave COMBAT examples (foe defeated / hit landed); nothing told the narrator a failed recall/knowledge/perception check means the info stays out of reach. The LLM is handed the mechanics string (carries "→ failure") but had no rule to honor it for checks | Added a prompt rule: on "→ failure" / a roll short of DC, do not narrate the player achieving/learning/recalling what they tried for | `tests/U169.failedCheckNarration.test.js` |
| F5 | CANON_HALLUCINATION | Lore-hound pressure → NPC invented a council seat count and the outpost's founding history / "first elder" | The npc-voice DEFLECT decision and the closing one-line guard (`server/npcVoicePrompt.js`) forbade only NAMES, leaving invented numbers/counts/dates/history allowed | Strengthened the DEFLECT instruction and the closing guard to forbid inventing ANY specific (numbers, counts, dates, names, titles, history) and steer toward pleading ignorance / hearsay | `tests/U168.npcVoiceNoInvention.test.js` |
